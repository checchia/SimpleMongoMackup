from distutils.core import setup
setup(
  name='smdb',
  packages = ['smdb'], # this must be the same as the name above
  version='0.0.1',
  description='A simple MongoDB backup',
  author='Checchia.NET - IT Solutions',
  author_email='dono@checchia.net',
  license='GPLv3',
  url='https://github.com/checchia/MongoBackup', # The project's main homepage.
  keywords = ['MongoDB', 'backup', 'backup Rotate'], # arbitrary keywords
  classifiers = [],
)
